//Exemplo
let mensagem;
if (nota >= 6) {
    mensagem = 'Aprovado'
} else {
    mensagem = 'Reprovado'
}

const mensagem = nota > 6 ? 'Aprovado' : 'Reprovado';

//Resposta

let mensagem 
if (temperatura >= 30) {
mensagem = 'Quente'
} else {
    mensagem = 'Agradavel'
}

const mensagem  = temperatura >= 30 ? 'Quente' : 'Reprovado'

let tipo;
if (numero % 2=== 0) {
    tipo= 'par'
} else {
    tipo= 'impar'
}

const mensagem = numero % 2 === 0? 'par' : 'Reprovado'

let saudacao 
if (hora < 12) {
    saudacao ='Bom dia'
} else {
    saudacao ='Boa tarde/boa noite'
}

const mensagem = hora < 12 ? 'Bom dia' : 'Boa noite'